# DeepSeek Chat - 项目架构文档

> 最后更新: 2026-06-04

---

## 1. 项目概述

**DeepSeek Chat** 是一个基于 DeepSeek API 的智能对话客户端，提供 **命令行交互模式**、**Web UI 模式** 和 **桌面应用模式** 三种使用方式。项目使用 Python 编写，前端采用纯 HTML/CSS/JavaScript，后端使用 Flask 框架。

### 1.1 核心功能

- 💬 多会话管理（WebUI 侧边栏历史列表）
- 🖥️ 三种运行模式：命令行、Web 浏览器、原生桌面窗口
- 🧠 长期记忆系统（记住/忘记指令，持久化到 JSON 文件）
- 📝 对话历史自动保存（Markdown 格式）
- 🔄 流式输出（SSE - Server-Sent Events）
- 🌓 深色/浅色主题切换
- ⚙️ 运行时配置热修改（通过 WebUI 设置对话框）
- 🔑 API 密钥、模型、温度等参数可配置

---

## 2. 目录结构

```
e:\PythonProjects\deepseek_chat\
├── app.py                          # Flask Web 应用（WebUI 后端）
├── deepseekchat.py                 # DeepSeek API 核心封装类
├── main.py                         # 主入口（CLI 模式）
├── desktop_app.py                  # 桌面应用包装（pywebview）
├── env_utils.py                    # 环境变量工具（API密钥管理）
├── config.yaml                     # 配置文件（使用 ${DEEPSEEK_API_KEY} 占位符）
├── generate_icon.py                # 图标生成工具（webp → ico，旧版）
├── convert_icon.py                 # 圆形图标生成工具（png → 圆形多尺寸 ico）
├── build_exe.bat                   # PyInstaller 打包脚本
├── DeepSeekChat.spec               # PyInstaller 配置文件
├── memories.json                   # 长期记忆持久化文件
├── requirements-desktop.txt        # 桌面版依赖
├── .env                            # 环境变量文件（含真实API密钥，已 gitignore）
├── icon.png                        # 应用图标源文件（PNG 格式）
├── whale.ico                       # 生成的 Windows 图标
├── templates/
│   └── chat.html                   # WebUI 前端页面（单页应用）
├── history/                        # 对话历史记录目录
│   ├── session_20260528_205615.md
│   ├── session_20260528_213243.md
│   ├── session_20260528_222908.md
│   └── session_20260529_111119.md
├── dist/                           # 打包输出目录
│   ├── deepseek_chat/              # 打包后的程序文件夹
│   └── deepseek_chat.7z            # 压缩包
└── 项目架构.md                      # 本文件
```

---

## 3. 文件功能详解

### 3.1 `config.yaml` — 全局配置文件

YAML 格式，配置项分层：

| 顶层键 | 子键 | 说明 | 默认值 |
|--------|------|------|--------|
| `api` | `api_key` | DeepSeek API 密钥（使用 `${DEEPSEEK_API_KEY}` 占位符，真实密钥存于 `.env`） | `${DEEPSEEK_API_KEY}` |
| `api` | `base_url` | API 基础地址 | `https://api.deepseek.com` |
| `model` | `name` | 模型名称 | `deepseek-v4-flash` |
| `model` | `reasoning_effort` | 推理努力度 | `high` |
| `model` | `temperature` | 温度参数 | `0.7` |
| `model` | `max_tokens` | 最大 Token 数 | `null` (不限) |
| `model` | `thinking_enabled` | 启用思维链 | `true` |
| `chat` | `system_prompt` | 系统提示词 | 专业耐心的助手，Markdown + LaTeX 格式 |
| `chat` | `save_history` | 是否保存历史 | `true` |
| `chat` | `history_dir` | 历史目录名 | `history` |
| `memory` | `enabled` | 启用记忆系统 | `true` |
| `memory` | `file` | 记忆文件路径 | `memories.json` |
| `memory` | `auto_apply` | 自动注入记忆 | `true` |
| `ui` | `theme` | 默认主题 | `light` |
| `ui` | `color_*` | 终端颜色代码 | ANSI 颜色码 |
| `ui` | `show_token_usage` | 显示 Token 用量 | `false` |

> **安全改进:** API 密钥不再明文存储在 `config.yaml` 中，改用 `${DEEPSEEK_API_KEY}` 占位符，通过 `env_utils.py` 从系统环境变量或 `.env` 文件解析。`.env` 文件已加入 `.gitignore`。

### 3.2 `deepseekchat.py` — 核心引擎

**类: `DeepSeekChat`**

这是项目的核心模块，封装了所有业务逻辑，可被 `main.py` 或 `app.py` 调用。

**初始化流程:**
1. 从 config 字典读取 API、模型、对话、UI、记忆配置
2. 创建 OpenAI 客户端
3. 从 `memories.json` 加载长期记忆
4. 构建 system messages（含记忆注入）
5. 创建会话历史 Markdown 文件

**主要方法:**

| 方法 | 说明 |
|------|------|
| `send_message(user_input)` | 发送消息，流式输出到终端，自动处理记忆命令 |
| `run_interactive()` | 启动交互式对话循环 |
| `clear_context()` | 清空对话上下文（保留 system prompt 和记忆） |
| `save_chat_history(title)` | 保存对话历史到 JSON 文件 |
| `_add_memory(content)` | 添加长期记忆（去重）并持久化，注入即时指令 |
| `_remove_memory(content_or_index)` | 按内容或序号删除记忆 |
| `_show_memories()` | 显示所有记忆 |
| `_rebuild_messages()` | 重新构建 messages 列表（保留历史，更新 system） |
| `_build_system_messages()` | 构建 system prompt + 记忆注入 |

**内置命令（交互模式）:**

| 命令 | 功能 |
|------|------|
| `exit` / `quit` | 退出对话 |
| `/clear` | 清空上下文 |
| `/save` | 保存对话历史 |
| `/memories` | 查看长期记忆 |
| `记住：xxx` / `记住:xxx` | 添加长期记忆 |
| `忘记：xxx` / `忘记:xxx` | 删除长期记忆（支持序号或内容匹配） |

**关键设计:**
- 记忆系统通过 `system` 角色消息注入上下文
- 添加记忆时会立即插入一条 `[系统即时指令]` 消息增强即时效果
- 所有对话记录以 Markdown 格式追加到 `history/session_*.md`

### 3.3 `main.py` — 命令行入口

**功能:** 解析命令行参数 → 加载配置 → 创建 `DeepSeekChat` 实例 → 启动交互或单次模式

**命令行参数:**

| 参数 | 简写 | 说明 |
|------|------|------|
| `--config` | `-c` | 指定配置文件路径 |
| `--api-key` | | API 密钥 |
| `--base-url` | | API 基础地址 |
| `--model` | `-m` | 模型名称 |
| `--temperature` | `-t` | 温度参数 |
| `--reasoning-effort` | | 推理努力度 (`low`/`medium`/`high`) |
| `--no-thinking` | | 禁用思维链 |
| `--system-prompt` | `-s` | 系统提示词 |
| `--no-history` | | 不保存对话历史 |
| `--single` | | 单次对话模式（发送一条消息后退出） |
| `--save` | | 单次模式时指定保存标识 |

**运行模式:**
- 默认: 交互模式（循环读取用户输入）
- `--single "消息"`: 单次问答模式

### 3.4 `app.py` — Flask Web 后端

**功能:** 提供 WebUI 的 REST API 和页面渲染。

**路由表:**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 渲染主页面 (`chat.html`) |
| GET | `/api/config` | 返回前端配置（模型列表） |
| GET/POST | `/api/model` | 获取/切换当前模型 |
| GET | `/api/sessions` | 列出所有历史会话 |
| POST | `/api/sessions/load` | 加载指定历史会话 |
| POST | `/api/sessions/new` | 创建新会话 |
| DELETE | `/api/sessions/<filename>` | 删除指定会话（防路径穿越） |
| POST | `/api/chat/<session_id>` | 发送消息，返回 SSE 流式响应 |
| GET/POST | `/api/settings` | 获取/保存配置（写入 config.yaml） |
| GET/POST | `/api/memories` | 获取/保存长期记忆列表 |
| DELETE | `/api/memories/<index>` | 删除指定序号的记忆 |
| GET | `/api/balance` | 查询 DeepSeek API 账户余额（支持 CNY/USD 切换） |

**关键设计:**
- 会话管理: 内存字典 `_sessions` + 文件持久化（Markdown）
- 流式输出: 使用 `Response` + `stream_with_context` + SSE 格式
- 配置热重载: 保存设置后重新加载 config.yaml 并更新全局变量
- `DATA_DIR` 处理: 支持 PyInstaller 打包后的路径重定向

**会话生命周期:**
1. 创建新会话 → 生成 `session_时间戳.md` 文件头
2. 用户发送消息 → 追加到 Markdown 文件 + 发送 API 请求
3. AI 回复 → SSE 流式推送到前端 + 追加到 Markdown 文件
4. 加载历史 → 解析 Markdown 文件恢复消息列表

### 3.5 `desktop_app.py` — 桌面应用包装

**功能:** 将 Flask WebUI 包装为原生桌面窗口（基于 `pywebview`）。

**流程:**
1. 寻找空闲端口
2. 守护线程启动 Flask
3. 等待 1 秒确保启动
4. 打开 pywebview 原生窗口（1100×750，最小 800×500）

**特性:**
- 自动读取 `config.yaml` 获取模型名作为窗口标题
- 使用 `whale.ico` 作为窗口图标（如果存在）
- 支持 PyInstaller 打包

### 3.6 `templates/chat.html` — 前端单页应用

**技术栈:** 原生 HTML + CSS + JavaScript（无任何前端框架依赖）

**UI 布局:**
```
┌──────────────────────────────────────────────┐
│  左侧边栏 (280px)  │       主区域            │
│  ┌───────────────┐ │ ┌──────────────────────┐│
│  │ 对话列表标题   │ │ │ 顶部栏 (模型切换/设置)││
│  │ [✚ 新对话]    │ │ ├──────────────────────┤│
│  ├───────────────┤ │ │                      ││
│  │ 会话项 1      │ │ │   聊天消息区域        ││
│  │ 会话项 2      │ │ │                      ││
│  │ 会话项 3      │ │ │                      ││
│  │ ...           │ │ ├──────────────────────┤│
│  ├───────────────┤ │ │ 输入区域 (textarea)   ││
│  │ Powered by    │ │ │ [➤]                  ││
│  │ DeepSeek      │ │ └──────────────────────┘│
│  └───────────────┘ │                         │
└──────────────────────────────────────────────┘
```

**前端功能模块:**

| 功能 | 说明 |
|------|------|
| 会话列表管理 | 加载、切换、删除历史会话 |
| 消息渲染 | 区分用户/AI 气泡；AI 消息自动解析 Markdown 代码块 |
| 代码块展示 | 浅灰背景独立区域，左上角语言标识，右上角「复制」按钮 |
| 复制功能 | 点击复制按钮将代码复制到剪贴板，显示「已复制」反馈 |
| 流式输出 | 基于 Fetch API + ReadableStream 解析 SSE，流结束后渲染代码块 |
| 主题切换 | 深色/浅色双主题（CSS 变量），持久化到 localStorage |
| 模型切换 | 运行时切换 deepseek-v4-flash / deepseek-v4-pro |
| Token 估算 | 前端粗略估算输入 Token 数 |
| 设置对话框 | 左侧导航栏 + 右侧面板布局，分 API/模型/对话/记忆/界面 五类 |
| 记忆管理 | 在设置中直接查看、添加、编辑、删除长期记忆 |
| 键盘快捷键 | Enter 发送，Shift+Enter 换行，Esc 关闭设置 |

**CSS 主题系统:** 使用 `:root` CSS 变量 + `.light-theme` 类切换，涉及 ~30 个变量（背景色、文字色、边框、气泡色等），带过渡动画。新增代码块组件样式：`.code-block` 容器、`.code-header` 顶栏、`.code-lang` 语言标识、`.copy-btn` 复制按钮（含 `copied` 状态）。

### 3.7 `generate_icon.py` — 图标生成工具（旧版）

**功能:** 将 `whale.webp` 转换为 Windows ICO 图标文件。

**流程:**
1. 读取 WebP 图片
2. 转换为 RGBA（保留透明）
3. 裁剪为正方形
4. 缩放到指定尺寸（默认 64×64）
5. 保存为 ICO 格式

### 3.8 `convert_icon.py` — 圆形图标生成工具（新版）

**功能:** 将 `icon.png` 裁剪为圆形并生成多尺寸 Windows ICO 图标文件。

**流程:**
1. 读取 `icon.png`（RGBA，带透明通道）
2. 扫描内容边界（跳过透明像素）
3. 裁剪内容区域并居中放置到正方形画布（含 5% 内边距）
4. 创建圆形蒙版（`ellipse` 绘制），应用蒙版裁剪为圆形
5. 缩放到 256/128/64/48/32/16 六个尺寸
6. 手动构建 ICO 文件格式（含全部 6 个尺寸）

**输出:** `whale.ico` — 圆形图标，被 `desktop_app.py` 和 `DeepSeekChat.spec` 引用作为窗口图标。

> **注意:** `build_exe.bat` 打包时优先执行 `convert_icon.py`，失败时回退到 `generate_icon.py`。

### 3.9 `env_utils.py` — 环境变量工具

**功能:** 统一管理 API 密钥等敏感信息，支持多级来源（优先级从高到低）：
1. 系统环境变量 (`os.environ`)
2. `.env` 文件（项目根目录，已 `.gitignore`）
3. `config.yaml` 中的原始值（含 `${...}` 占位符时跳过）

**主要函数:**

| 函数 | 说明 |
|------|------|
| `resolve_config(raw_config)` | 解析配置字典中的 `${VAR}` 占位符，替换为环境变量值 |
| `ensure_api_key(config)` | 确保 API 密钥可用，首次运行提示用户输入 |
| `get_api_key()` | 从环境变量或 `.env` 获取 API 密钥 |
| `save_api_key_to_env(key)` | 将 API 密钥写入 `.env` 文件 |
| `save_base_url_to_env(url)` | 将 API 地址写入 `.env` 文件 |
| `_is_valid_api_key(key)` | 检查密钥是否有效（不为空、不是占位符） |

**安全设计:**
- `config.yaml` 使用 `${DEEPSEEK_API_KEY}` 占位符，可安全提交到 Git
- 真实密钥仅存储在本地的 `.env` 文件（已 `.gitignore`）
- WebUI 设置页保存 API 密钥时自动写入 `.env`，不修改 `config.yaml`
- CLI 首次运行自动检测 `.env`，不存在时创建含 `YOUR-API-KEY` 占位符的模板

### 3.10 `build_exe.bat` — 打包脚本

**步骤:**
1. [0/5] 清理旧的构建目录
2. [1/5] 安装依赖 (`requirements-desktop.txt`)
3. [2/5] 生成鲸鱼图标（优先运行 `convert_icon.py`，失败则回退 `generate_icon.py`）
4. [3/5] 安装 PyInstaller
5. [4/5] 打包（`--onedir --windowed` 模式）

**PyInstaller 打包关键参数:**
- `--icon=whale.ico`: 使用圆形鲸鱼图标
- `--onedir`: 生成文件夹模式
- `--windowed`: 无控制台窗口
- `--add-data "templates;templates"`: 包含前端模板
- `--hidden-import`: 显式声明所有隐式导入
- `--collect-all webview flask`: 收集所有数据文件

### 3.11 `DeepSeekChat.spec` — PyInstaller 配置文件

预先生成的 spec 文件，包含相同的打包配置，可通过 `pyinstaller DeepSeekChat.spec` 直接构建。

### 3.12 `memories.json` — 记忆持久化文件

JSON 数组格式，每条记忆为一个字符串。当前为空：`[]`

---

## 4. 数据流

### 4.1 消息发送流程（WebUI 模式）

```
用户输入 → chat.html (Fetch API) 
    → POST /api/chat/<session_id> (Flask)
        → app.py 处理: 追加用户消息到 Markdown
        → OpenAI API 流式请求
        → SSE 流式返回 (data: {content, done})
    → chat.html 解析 SSE → 实时渲染消息
    → 流结束后: 追加 AI 回复到 Markdown + 更新会话列表
```

### 4.2 配置保存流程

```
WebUI 设置对话框 → POST /api/settings
    → app.py: 更新 config.yaml + 热重载全局变量
    → 返回 {success: true}
    → 前端: 显示 Toast 提示 + 刷新模型列表 + 应用主题
```

### 4.3 记忆系统数据流

```
用户输入 "记住：xxx"
    → deepseekchat.py: _is_remember_cmd() 检测
    → _add_memory(): 去重 → 追加到 self.memories → 写入 memories.json
    → _rebuild_messages(): 重新构建 system messages（含记忆注入）
    → 插入即时指令消息增强效果

下次启动:
    → __init__ → _load_memories() → _build_system_messages() → 记忆自动注入
```

---

## 5. 依赖关系

```
requirements-desktop.txt:
├── Flask>=3.0          # Web 框架
├── openai>=1.0         # OpenAI API 客户端
├── PyYAML>=6.0         # YAML 配置解析
├── pywebview>=4.0      # 原生桌面窗口（仅桌面模式需要）
├── Pillow>=10.0        # 图标生成（仅打包时需要）
└── requests>=2.0       # API 余额查询（/api/balance）

# PyInstaller（仅打包时需要，不写在 requirements 中）
```

**模块依赖关系:**
```
main.py → deepseekchat.py (核心类)
app.py  → deepseekchat.py (复用设计，但独立实现 API)
app.py  → env_utils.py (API密钥/环境变量解析)
desktop_app.py → app.py (启动 Flask)
templates/chat.html ← app.py (Flask 渲染)
```

---

## 6. 三种运行模式对比

| 特性 | CLI 模式 (`main.py`) | WebUI 模式 (`app.py`) | 桌面模式 (`desktop_app.py`) |
|------|---------------------|----------------------|---------------------------|
| 启动命令 | `python main.py` | `python app.py` | `python desktop_app.py` |
| 交互方式 | 终端输入 | 浏览器 `localhost:5000` | 原生桌面窗口 |
| 记忆命令 | ✅ `记住：/忘记：` | ❌ (未实现) | ❌ (未实现) |
| 多会话 | ❌ (单次运行一个会话) | ✅ 侧边栏管理 | ✅ (同 WebUI) |
| 设置界面 | ❌ (命令行参数) | ✅ 图形化设置对话框 | ✅ (同 WebUI) |
| 主题切换 | ❌ (终端颜色) | ✅ 深色/浅色 | ✅ (同 WebUI) |
| 历史查看 | Markdown 文件 | 侧边栏列表 + 加载 | ✅ (同 WebUI) |
| 流式输出 | 终端打印 | SSE 推送 | SSE + WebView |

---

## 7. 注意事项 & 已知设计

1. **WebUI 模式未实现记忆命令**：`app.py` 中的聊天逻辑没有检测"记住：/忘记："命令，WebUI 用户无法通过对话管理记忆，只能通过设置页面启用/禁用记忆功能。
2. **`app.py` 和 `deepseekchat.py` 有代码重复**：两处都实现了相似的记忆加载、system message 构建、Markdown 历史写入逻辑。`app.py` 是独立实现的，没有复用 `DeepSeekChat` 类。
3. **前端无框架**：纯原生 JS，适合轻量运行，但扩展复杂功能时需手动管理状态。
4. **API 密钥安全**：已通过 `env_utils.py` + `.env` 文件系统解决 - `config.yaml` 仅存 `${DEEPSEEK_API_KEY}` 占位符，真实密钥存储在 `.env`（已 `.gitignore`），支持系统环境变量 → `.env` 文件 → config.yaml 三级解析。
5. **历史文件格式**：Markdown 格式，解析依赖正则表达式 `### [用户/DeepSeek]` 块标记。
6. **打包后数据目录**：PyInstaller 打包后，配置/历史/记忆文件将存储在 exe 所在目录（`DATA_DIR` 通过 `sys.frozen` 判断）。
7. **API 余额查询**：通过 `/api/balance` 路由调用 DeepSeek `/user/balance` API，前端点击余额数字可切换 CNY/USD 显示，每 3 次对话自动刷新。
8. **图标文件**：使用 `icon.png`（带透明底的圆形设计）作为源文件，通过 `convert_icon.py` 裁剪为圆形并转换为 `whale.ico`（含 256/128/64/48/32/16 六种尺寸），供桌面窗口和打包使用。
